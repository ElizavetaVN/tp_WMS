@model IEnumerable<Domain.Entities.Realization>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a class="btn btn-primary" asp-controller="Realization" asp-action="Create">Create</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Тип
            </th>
            <th>
                Заказ
            </th>
            <th>
                Контрагент
            </th>
            <th>
                Склад
            </th>
            <th>
                Номенклатура
            </th>
            <th>
                Дата
            </th>
            <th>
                Количество
            </th>
            <th>
                Единицы измерения
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RealizationType.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Partners.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Warehouses.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Products.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Units.Name)
            </td>
            <td>
                <a class="btn btn-sm btn-primary" asp-controller="Realization" asp-action="Update" asp-route-Id="@item.Id">Update</a>
            </td>
        </tr>
}
    </tbody>
</table>
